# Random sample library

Given a list of hub urls for getting stream urls in a roulette manner, it will
periodically fetch a random stream and add the sample of random length to a
sample library for playback.

To be used with the data roulette tool and multi-channel looper player in the
_noisecrypt system.
