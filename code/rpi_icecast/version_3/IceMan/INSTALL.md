-   sqlite3
-   pyo (custom) -> deps
-   python3
-   pip3
-   virtualenv
-   node12
-   darkice -> deps
-   icecast2 -> deps
-   jackd + jack_connect, jack_lsp -> deps
-   npm pkgs
-

steps:

-   wget/curl download this dir master branch
-   install system dependencies
-   create venv for scripts/pythonPlayer (install in scripts dir)
-   download and build pyo (install in scripts dir)
-   install python deps
-   install node deps
-   install symlink to exec?
-   make build dir exec?
