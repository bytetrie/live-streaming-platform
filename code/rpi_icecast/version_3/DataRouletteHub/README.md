# Data Roulette Hub

A kind of hub-based pluggable server for receiving validatable data that can
then be randomly returned from a pool.

To be used to support the requirement for private hubs of Icecast stream URLs in
the _noisecrypt system.
